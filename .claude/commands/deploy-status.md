Check the GitHub Actions deployment workflow status for this repository.

Run `gh run list --repo seb-thomas/scaling-engine --limit 5` to see recent workflow runs.

If there's a failed or in-progress deployment, check the logs with `gh run view <run_id> --repo seb-thomas/scaling-engine --log-failed` for failures or `gh run view <run_id> --repo seb-thomas/scaling-engine` for details.
